apiVersion: v1
kind: Service
metadata:
    name: robcruzdproybaseangmvn
spec:
    type: LoadBalancer
    ports:
    - port: 80 
      targetPort: 8080
    selector:
        app: robcruzdproybaseangmvn